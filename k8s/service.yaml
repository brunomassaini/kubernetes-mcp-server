apiVersion: v1
kind: Service
metadata:
  name: kubernetes-mcp-server
  namespace: default
spec:
  selector:
    app: kubernetes-mcp-server
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
# For external access, change type to LoadBalancer or add an Ingress/Route.
